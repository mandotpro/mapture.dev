# Release Scripts

This directory contains scripts used by release automation and distribution packaging:

- building release archives
- calculating the next semantic version
- printing the next stable release plan and PR title
- generating Homebrew formulae
- syncing the Homebrew tap

Developer-local build, test, lint, and repo helper scripts stay in the parent `scripts/` directory.
